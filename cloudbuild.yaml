steps:
- name: 'gcr.io/cloud-builders/gcloud'
  args:
  - functions
  - deploy
  - pipeline
  - --source=.
  - --trigger-topic=dailyscheduled
  - --runtime=python39
  - --memory=1024MB
  - --timeout=540s
